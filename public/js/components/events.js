const React = require('react');
const auth = require('../auth');

const Events = React.createClass({

  render : function() {
    return (
      <div>
        <h1>My events</h1>
      </div>
    )
  }
});

module.exports = Events;