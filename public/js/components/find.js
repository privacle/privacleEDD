const React = require('react');
const auth = require('../auth');

const Find = React.createClass({

  render : function() {
    return (
      <div>
        <h1>Find stuff</h1>
      </div>
    )
  }
});

module.exports = Find;