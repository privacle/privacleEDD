const React = require('react');
const auth = require('../auth');

const Signup = React.createClass({

  render : function() {
    return (
      <div>
        <h1>sign up form</h1>
      </div>
    )
  }
});

module.exports = Signup;