const React = require('react');
const auth = require('../auth');

const Create = React.createClass({

  render : function() {
    return (
      <div>
        <h1>Create event</h1>
      </div>
    )
  }
});

module.exports = Create;